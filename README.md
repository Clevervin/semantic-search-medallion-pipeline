# semantic-search-medallion-pipeline
Production-grade data engineering pipeline implementing Medallion Architecture for semantic search. Ingests unstructured Amazon e-commerce data using PySpark, applies recursive semantic chunking, and stores vector embeddings in PostgreSQL (pgvector).
